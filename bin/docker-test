#!/usr/bin/env sh

set -e

if [ $RAILS_ENV == "test" ]
then
  docker-compose up -f docker-compose.test.yml -d
  docker-compose -f docker-compose.test.yml exec web bin/rspec ${@:-spec}
else
  docker-compose up -d
  docker-compose exec web bin/rspec ${@:-spec}
fi
